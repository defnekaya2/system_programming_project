## Application Usage (Week 3)

The application currently provides the core structure for a Student Information System.
User interaction is handled through the main application entry point.

Student-related operations are implemented in a service-based structure.
Full CRUD functionality is planned for the next development phase.
Note: This documentation is being incrementally updated based on weekly development progress.

  GNU nano 6.2                        MANUEL.md                                 
Student Information System – User Manual

This application is a command-line based Student Information System developed i>

The system allows users to add new student information, list all stored student>

To run the system, Docker and Docker Compose must be installed on the host mach>

Start the system with the following command:
docker compose up -d

The application runs as a CLI and guides the user through the available operati>

To stop the application and remove the containers, use the following command:
docker compose down

